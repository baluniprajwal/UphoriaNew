export interface EventItem {
  id: number;
  title: string;
  category: string;
  image: string;
  description: string;
  date: string;
}

export interface NavLink {
  label: string;
  href: string;
}
